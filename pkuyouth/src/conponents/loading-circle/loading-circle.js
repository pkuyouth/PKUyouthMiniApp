// conponents/loading-circle/loading-circle.js
Component({
	properties: {

	},
	data: {
		animation: {},
	},
	created() {
		/*
		this.animation = wx.createAnimation({
			duration: 2000,
			timingFunction: 'linear',
		});
		this.loop = 1;
		this.circleAnimation();
		 */
	},
	methods: {
		/*
		circleAnimation() {
			this.setData({
				animation: this.animation.rotate(360*this.loop++).step().export(),
			});
			setTimeout(this.circleAnimation.bind(this),2010);
		 */
		
	}
})
